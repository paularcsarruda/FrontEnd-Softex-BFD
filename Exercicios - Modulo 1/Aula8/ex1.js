// 1. Declare uma variável para guardar o nome do usuário (pode mudar)
let nome = "Maria"; 
// Justificativa: o nome do usuário pode ser alterado durante a execução, então usamos `let`.

// 2. Declare uma variável para guardar a data de nascimento (não muda)
const dataNascimento = "10/02/2000"; 
// Justificativa: a data de nascimento não muda, então usamos `const` para proteger a variável.

// 3. Declare uma variável para o saldo bancário (pode mudar)
let saldo = 1000; 
// Justificativa: o saldo pode ser alterado com depósitos ou saques, então usamos `let`.

// 4. Declare uma variável para o número de CPF (não muda)
const cpf = "123.456.789-00"; 
// Justificativa: o CPF não muda, portanto usamos `const`.
